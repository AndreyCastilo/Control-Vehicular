<!-- IMG1 -->
<div id="modal_imagenPreviewCedulaEditar" class="modal_image">

    <!-- The Close Button -->
    <span class="close-image" onclick="document.getElementById('modal_imagenPreviewCedulaEditar').style.display = 'none'">&times;</span>

    <!-- Modal Content (The Image) -->
    <img class="modal-content-image" id="img01">

    <!-- Modal Caption (Image Text) -->
    <div id="caption"></div>
</div>

<!-- IMG2 -->
<div id="modal_imagenPreviewLicenciaEditar" class="modal_image">

    <!-- The Close Button -->
    <span class="close-image" onclick="document.getElementById('modal_imagenPreviewLicenciaEditar').style.display = 'none'">&times;</span>

    <!-- Modal Content (The Image) -->
    <img class="modal-content-image" id="img02">

    <!-- Modal Caption (Image Text) -->
    <div id="caption"></div>
</div>

<!-- IMG1 -->
<div id="modal_imagenPreviewCedula" class="modal_image">

    <!-- The Close Button -->
    <span class="close-image" onclick="document.getElementById('modal_imagenPreviewCedula').style.display = 'none'">&times;</span>

    <!-- Modal Content (The Image) -->
    <img class="modal-content-image" id="img03">

    <!-- Modal Caption (Image Text) -->
    <div id="caption"></div>
</div>

<!-- IMG2 -->
<div id="modal_imagenPreviewLicencia" class="modal_image">

    <!-- The Close Button -->
    <span class="close-image" onclick="document.getElementById('modal_imagenPreviewLicencia').style.display = 'none'">&times;</span>

    <!-- Modal Content (The Image) -->
    <img class="modal-content-image" id="img04">

    <!-- Modal Caption (Image Text) -->
    <div id="caption"></div>
</div>



